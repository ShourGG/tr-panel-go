# Terraria Panel Configuration

# Server Port
PORT=8800

# Environment (development/production)
ENV=development

# JWT Secret (required, at least 32 chars)
JWT_SECRET=change-this-to-a-long-random-string-at-least-32-chars

# Comma-separated allowed origins for CORS and WebSocket
# Production example: ALLOWED_ORIGINS=https://panel.example.com
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8800,http://127.0.0.1:8800

# ========== Download Optimization Settings ==========

# Use GitHub Mirror (recommended for users in China)
# Set to "true" to enable GitHub mirror acceleration
USE_GITHUB_MIRROR=true

# GitHub Mirror URL
# Available mirrors:
# - https://ghproxy.com/ (recommended, fast and stable)
# - https://gh-proxy.com/
# - https://mirror.ghproxy.com/
GITHUB_MIRROR_URL=https://ghproxy.com/

# Download Retry Count
# Number of retries when download fails
DOWNLOAD_RETRIES=3

# Download Timeout (seconds)
# Timeout for each download attempt
DOWNLOAD_TIMEOUT=300

# Short-lived download ticket TTL (seconds)
# Used by backup / file downloads to avoid exposing long-lived login tokens in URLs
DOWNLOAD_TICKET_TTL=90

# Enable accelerated static downloads through Nginx X-Accel-Redirect
# Disabled by default; when disabled the panel falls back to Go direct download
DOWNLOAD_ACCEL_ENABLED=false

# Download acceleration type
# Currently supported: nginx
DOWNLOAD_ACCEL_TYPE=nginx

# Internal Nginx location prefix for backup files
DOWNLOAD_ACCEL_BACKUP_PREFIX=/__downloads/backups

# Internal Nginx location prefix for managed data files
DOWNLOAD_ACCEL_DATA_PREFIX=/__downloads/data

# Enable Multi-threaded Download (experimental)
# Set to "true" to enable multi-threaded download
# Note: This feature is experimental and may not work with all servers
ENABLE_MULTI_THREAD=false
